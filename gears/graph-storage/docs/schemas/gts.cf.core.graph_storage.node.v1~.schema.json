{
  "$id": "gts://gts.cf.core.graph_storage.node.v1~",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Base graph node. Carries the GTS instance envelope (id, type) plus the producer-authored fields; tenant, timestamps, actor and revision are gear-assigned and are deliberately not part of the type.",
  "x-gts-abstract": true,
  "x-gts-traits-schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "family": {
        "type": "string",
        "enum": [
          "owned",
          "reference",
          "phantom"
        ],
        "description": "Storage semantics of the node."
      },
      "scope_managed": {
        "type": "boolean",
        "default": true,
        "description": "Whether rows of this type participate in producer-scoped replacement re-sync."
      },
      "emit_events": {
        "type": "boolean",
        "default": false,
        "description": "Whether CREATE/UPDATE/DELETE events are published for this type."
      },
      "index": {
        "type": "array",
        "items": {
          "type": "string",
          "format": "json-pointer"
        },
        "default": [],
        "description": "Payload paths backed by a JSONB index and admissible in $filter."
      },
      "full_text_search": {
        "type": "array",
        "items": {
          "type": "string",
          "format": "json-pointer"
        },
        "default": [],
        "description": "Paths composed into the node tsvector."
      },
      "vector_search": {
        "type": "array",
        "items": {
          "type": "string",
          "format": "json-pointer"
        },
        "default": [],
        "description": "Paths composed into the embedding input."
      }
    },
    "required": [
      "family"
    ]
  },
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "description": "Producer-supplied stable key, unique per tenant. The ingest DTO spells it node_key; it is the GTS instance id."
    },
    "type": {
      "type": "string",
      "format": "gts-type-id",
      "x-gts-ref": "gts.cf.core.graph_storage.node.v1~",
      "description": "The node's registered GTS type."
    },
    "name": {
      "type": "string",
      "maxLength": 1024,
      "description": "Display name."
    },
    "payload": {
      "type": "object",
      "additionalProperties": true,
      "description": "Typed attributes; derived types refine this member."
    },
    "content": {
      "type": "string",
      "description": "Long-form text; chunked and indexed, bounded by content_max_bytes."
    }
  },
  "required": [
    "id",
    "type"
  ]
}
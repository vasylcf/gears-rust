// GENERATED by toolkit-contract-protogen — DO NOT EDIT BY HAND.
// Regenerate via the SDK's `gen_grpc_proto` example.

syntax = "proto3";
package cluster.lock.v1;

// DistributedLockApi — gear: cluster, version: v1
service DistributedLockApi {
  // unary
  rpc TryLock(TryLockRequest) returns (LockAcquired) {
    option idempotency_level = IDEMPOTENCY_UNKNOWN;
  }
  // unary
  rpc Lock(LockRequest) returns (LockAcquired) {
    option idempotency_level = IDEMPOTENCY_UNKNOWN;
  }
  // IDEMPOTENT
  rpc Renew(LeaseRef) returns (RenewResponse) {
    option idempotency_level = IDEMPOTENT;
  }
  // IDEMPOTENT
  rpc Release(LeaseRef) returns (ReleaseResponse) {
    option idempotency_level = IDEMPOTENT;
  }
}

message LeaseRef {
  optional string client_request_id = 1;
  string profile = 2;
  LeaseToken token = 3;
  optional uint64 ttl_ms = 4;
}

message LeaseToken {
  uint64 fence = 1;
  string name = 2;
  string owner = 3;
}

message LockAcquired {
  LeaseToken token = 1;
}

message LockRequest {
  optional string client_request_id = 1;
  string name = 2;
  string profile = 3;
  uint64 timeout_ms = 4;
  uint64 ttl_ms = 5;
}

message ReleaseResponse {
  uint64 generation = 1;
}

message RenewResponse {
  uint64 generation = 1;
}

message TryLockRequest {
  optional string client_request_id = 1;
  string name = 2;
  string profile = 3;
  uint64 ttl_ms = 4;
}


# Technical Design — {Gear Name}

<!-- Structure derived from gears-rust docs/spec-templates/gears-sdlc/DESIGN/template.md,
     specialized for gears: adds Gear Structure (crates, layers, registration) and
     Domain Model and GTS Types; storage section is mandatory (state "stateless" explicitly). -->

## Table of Contents

<!-- generated by `cfs toc` -->

## 1. Architecture Overview

### 1.1 Architectural Vision

{2-3 paragraphs: technical approach and how it satisfies the requirements.}

### 1.2 Architecture Drivers

**ADRs**: `cpt-{system}-adr-{slug}`{, one entry per gear ADR}

#### Functional Drivers

| Requirement | Design Response |
|-------------|------------------|
| `cpt-{system}-fr-{slug}` | {How the design addresses it} |

#### NFR Allocation

| NFR ID | Allocated To | Design Response | Verification |
|--------|--------------|-----------------|--------------|
| `cpt-{system}-nfr-{slug}` | {component/mechanism} | {realization} | {how verified} |

## 2. Gear Structure

{Crates and layers per the canonical layout. Name the SDK crate, the gear crate, capabilities declared in the gear macro, and the registration surface (workspace member, server registration, feature flag, unused-dependency exemption, e2e config).}

| Crate | Purpose |
|-------|---------|
| `cf-gears-{name}-sdk` | {public contract} |
| `cf-gears-{name}` | {implementation: api/ domain/ infra/} |

**Gear macro declaration**: name, capabilities `{...}`, deps `{...}`.

## 3. Domain Model and GTS Types

{Entities and their GTS type declarations; the error catalog. Reference schema files rather than embedding bodies.}

| Entity / Type | GTS ID | Where declared |
|---------------|--------|----------------|
| {Entity} | `gts.{vendor}.{package}.{ns}.{type}.v1~` | {sdk gts module / schemas dir} |

## 4. Component Model

### {Component Name}

- [ ] `p2` - **ID**: `cpt-{system}-component-{slug}`

**Why it exists**: {problem it solves}
**Responsibility scope**: {what it owns}
**Responsibility boundaries**: {what it delegates}
**Related components**: {IDs and relationship types}

## 5. API Surface

{SDK traits with one-line semantics; REST endpoint overview table; plugin seam traits. Contracts live in code and schema files — reference them.}

| Kind | Name | Consumers | Notes |
|------|------|-----------|-------|
| SDK trait | {TraitV1} | {who} | {semantics} |
| REST | {METHOD /path} | {actor} | {stability} |
| Plugin seam | {SeamV1} | {vendors} | {default impl} |

## 6. Dependencies

| Dependency | Interface | Purpose |
|------------|-----------|---------|
| {gear} | {SDK client / plugin} | {why} |
| {toolkit lib} | {workspace dep} | {why} |

## 7. Interactions

#### {Sequence Name}

**ID**: `cpt-{system}-seq-{slug}`

```mermaid
sequenceDiagram
    {participants and flow}
```

**Description**: {what this sequence accomplishes}

## 8. Storage

{Tables/entities with secure-ORM scoping columns, migrations plan — or the explicit statement "This gear is stateless." Reference entity files rather than embedding DDL.}

| Table | Purpose | Tenant scoping |
|-------|---------|----------------|
| {table} | {purpose} | {scoping column} |

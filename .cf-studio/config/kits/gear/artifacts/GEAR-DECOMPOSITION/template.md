# Decomposition — {Gear Name}

<!-- The phase contract: every phase leaves the workspace buildable, runnable, and tested.
     Phases map to the golden path: SDK first, gear host next, seams and features after. -->

## Table of Contents

<!-- generated by `cfs toc` -->

## Overview

{Delivery strategy: phase ordering rationale, dependencies between phases, what is deliberately deferred. Reference the golden-path sequence (specs → SDK → gear host + reference plugin → features).}

## Phases

### Phase {N}: {Phase Name}

- [ ] `p1` - **ID**: `cpt-{system}-phase-{slug}`

#### Scope

{What this phase delivers. Map to requirement and design element IDs: `cpt-{system}-fr-{slug}`, `cpt-{system}-component-{slug}`.}

#### Completion Criteria

- {Verifiable criterion 1 — the workspace builds}
- {Verifiable criterion 2 — new tests pass; existing tests stay green}
- {Verifiable criterion 3 — phase-specific observable behavior}

#### Gate

```sh
{the exact deterministic command(s), e.g. make gear-ci GEAR={name}}
```

{Repeat the phase section per phase. The final phase's gate is the full local gate plus the end-to-end run.}
